enum Settings {
    static let previewText = Setting(key: "previewText",
                                     default: "The quick brown fox jumps over the lazy dog")
    static let previewFontSize = Setting(key: "previewFontSize",
                                         default: 14.0)
}
